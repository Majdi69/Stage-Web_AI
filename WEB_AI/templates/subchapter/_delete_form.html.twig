<form method="post" action="{{ path('app_subchapter_delete', {'id': subchapter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subchapter.id) }}">
    <button class="btn">Delete</button>
</form>
