{% extends'baseFront.html.twig' %}
{% block body %}
   
    <main id="main">
        <h1>{{ subject.title }}</h1>
        <ul>
            {% for chapter in chapters %}
                <li><a href="{{ path('show_chapter', {'id':level.id,'subjectId':subject.id,'chapterId': chapter.id}) }}">{{ chapter.title }}</a></li>
            {% endfor %}
        </ul>
    </main>
{% endblock %}
